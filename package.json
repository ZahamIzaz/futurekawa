{
  "name": "futurekawa-bloc4",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "npm run test --workspace=backend-country && npm run test --workspace=backend-central && npm run test --workspace=frontend",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0"
  }
}
