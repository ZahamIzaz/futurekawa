# Développement local uniquement
# En Docker, ces variables sont injectées par docker-compose

PORT=3000
# Pour le dev local, le backend pays tourne sur localhost:3001
BRAZIL_BACKEND_URL=http://localhost:3001
